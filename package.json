{
  "name": "smartcontent-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-platform social media content management with AI",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "npm run build --prefix frontend",
    "netlify:build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "setup": "npm install && npm install --prefix frontend"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "@aws-sdk/client-s3": "^3.1131.0",
    "@aws-sdk/s3-request-presigner": "^3.1131.0"
  },
  "devDependencies": {
    "netlify-cli": "^27.5.2"
  },
  "keywords": [
    "social-media",
    "content-management",
    "ai",
    "instagram",
    "automation"
  ],
  "author": "Zafer Yildiz",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
